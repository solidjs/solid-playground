diff --git a/node_modules/vite/dist/node/chunks/dep-5c642f9e.js b/node_modules/vite/dist/node/chunks/dep-5c642f9e.js
index 0acaf4a..ca00069 100644
--- a/node_modules/vite/dist/node/chunks/dep-5c642f9e.js
+++ b/node_modules/vite/dist/node/chunks/dep-5c642f9e.js
@@ -45622,32 +45622,33 @@ function webWorkerPlugin(config) {
             }
             let url;
             if (isBuild) {
-                if (query.inline != null) {
-                    // bundle the file as entry to support imports and inline as base64
-                    // data url
-                    const rollup = require('rollup');
-                    const bundle = await rollup.rollup({
-                        input: cleanUrl(id),
-                        plugins: config.plugins
-                    });
-                    try {
-                        const { output } = await bundle.generate({
-                            format: 'es',
-                            sourcemap: config.build.sourcemap
-                        });
-                        url = `data:application/javascript;base64,${Buffer.from(output[0].code).toString('base64')}`;
-                    }
-                    finally {
-                        bundle.close();
-                    }
-                }
-                else {
-                    // emit as separate chunk
-                    url = `__VITE_ASSET__${this.emitFile({
-                        type: 'chunk',
-                        id: cleanUrl(id)
-                    })}__`;
-                }
+                 // bundle the file as entry to support imports
+				 const rollup = require('rollup');
+				 const bundle = await rollup.rollup({
+					 input: cleanUrl(id),
+					 plugins: config.plugins
+				 });
+				 try {
+					 const { output } = await bundle.generate({
+						 format: 'iife',
+						 sourcemap: config.build.sourcemap
+					 });
+					 if (query.inline != null) {
+						 // inline as base64 data url
+						 url = `data:application/javascript;base64,${Buffer.from(output[0].code).toString('base64')}`;
+					 }
+					 else {
+						 const fileName = path__default.parse(cleanUrl(id)).name;
+						 url = `__VITE_ASSET__${this.emitFile({
+							 type: 'asset',
+							 name: `${fileName}.js`,
+							 source: output[0].code
+						 })}__`;
+					 }
+				 }
+				 finally {
+					 bundle.close();
+				 }
             }
             else {
                 url = await fileToUrl(cleanUrl(id), config, this);
